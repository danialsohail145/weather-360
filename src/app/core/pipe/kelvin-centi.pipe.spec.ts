import { KelvinCentiPipe } from './kelvin-centi.pipe';

describe('KelvinCentiPipe', () => {
  it('create an instance', () => {
    const pipe = new KelvinCentiPipe();
    expect(pipe).toBeTruthy();
  });
});
